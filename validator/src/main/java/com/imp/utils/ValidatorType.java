package com.imp.utils;

/**
 * Created by IMP(郑和明)
 * date is 2017/5/1.
 */

public enum ValidatorType {

   SUCCESS,
   NULL,
   DATE,
   TYPE,
   EMAIL,
   URL,
   TELEPHONE,
   STRING_MAX_LENGTH,
   STRING_MIN_LENGTH,
   STRING_CHAR,
   INTEGER_MAX_LENGTH,
   INTEGER_MIN_LENGTH,
   LONG_MAX_LENGTH,
   LONG_MIN_LENGTH
}
